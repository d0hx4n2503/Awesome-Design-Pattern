---
title: "享元"
slug: "flyweight"
group: "structural"
groupLabel: "结构型"
source: "patterns/structural/flyweight/README.md"
---

# 享元

## 意图

用 享元 模式把变化点放到清晰的抽象边界后面。

## 问题

当代码开始因为不同规则、状态、提供商或结构而出现大量条件分支时，稳定流程会被迫理解太多细节。

## 核心思想

享元 的核心是让调用方依赖稳定契约，把真正变化的部分移动到独立对象或协作结构中。

## 实战视角

不要为了“用了设计模式”而使用 享元。它应该解决真实的变化压力：减少错误依赖、降低未来修改成本，并让测试边界更清晰。

## 真实场景

- TypeScript 项目中的结构型职责正在出现多个变体。
- 同一个决策点周围开始出现越来越多条件分支。
- 团队需要一种新人也能快速理解的运行时结构。

## 适用场景

- 已经存在多个真实变体，并且这些变体会继续增长。
- 调用方不应该知道 享元 背后的具体实现细节。
- 引入该模式后，新增规则、状态、产品或协作者不需要修改稳定流程。

## 不适用场景

- 当前只有一个实现，而且短期内没有真实变化压力。
- 一个普通函数、对象字面量、配置表或依赖注入已经足够清晰。
- 引入该模式会让运行时流程更难追踪，而不是更容易维护。

## 设计检查清单

- 先看调用方真正需要什么契约。
- 抽象命名应来自业务语义，而不只是模式名称。
- 保持具体类小而聚焦。
- 通过公共接口测试行为，而不是测试私有细节。
- 如果模式让代码更难读，应回到更简单的设计。

## 常见错误

- 只有假想变化点就提前引入模式。
- 创建只包了一层同名类的空抽象。
- 隐藏运行时流程，导致调试更困难。
- 为了展示模式知识而不是解决真实问题。

## 测试建议

- 单独测试每个具体行为或协作者。
- 用 fake implementation 测试调用方，证明边界有价值。
- 补充与选用该模式原因相关的失败路径和边界情况。

## TypeScript 实现

该模式目录中的 `index.ts` 提供可运行的 TypeScript 示例。建议结合对应测试一起阅读，重点关注调用方依赖的抽象边界，而不是具体类的名字。
